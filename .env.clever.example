# Variables d'environnement pour Clever Cloud
# NE PAS COMMITER CE FICHIER AVEC VOS VRAIES VALEURS
# Configurez ces variables dans la console Clever Cloud ou via Clever Tools CLI

# === OBLIGATOIRES ===

# Base de données MongoDB
# Si vous utilisez l'add-on MongoDB de Clever Cloud, utilisez MONGODB_ADDON_URI
# Sinon, utilisez votre propre URI MongoDB
MONGO_URI=mongodb://user:password@host:port/thot_production
# OU
# MONGODB_ADDON_URI=mongodb://... (fourni automatiquement par l'add-on)

# JWT Secret - DOIT être une clé très sécurisée en production
JWT_SECRET=votre_clé_secrète_très_longue_et_aléatoire_ici_minimum_32_caractères

# Environnement
NODE_ENV=production

# Port - Défini automatiquement par Clever Cloud sur 8080
# NE PAS MODIFIER sauf si vous savez ce que vous faites
PORT=8080

# URLs
# URL de votre frontend (pour CORS)
CLIENT_URL=https://votre-frontend.com

# URL de base de votre API
API_BASE_URL=https://app_8a0fbd58-42ee-4661-bb09-fd1d484f8ad8.cleverapps.io

# === OPTIONNELLES ===

# Redis (si vous utilisez l'add-on Redis)
# IMPORTANT: L'application fonctionne sans Redis (elle utilisera la mémoire pour le rate limiting)
# Pour ajouter Redis: clever addon create redis --plan dev
# Les variables suivantes seront alors configurées automatiquement:
# REDIS_HOST=... (fourni automatiquement par l'add-on)
# REDIS_PORT=... (fourni automatiquement par l'add-on)
# REDIS_PASSWORD=... (fourni automatiquement par l'add-on)
# REDIS_URL=redis://... (fourni automatiquement par l'add-on)

# Sentry pour le monitoring des erreurs
# SENTRY_DSN=https://...@sentry.io/...

# AWS S3 pour le stockage cloud (si utilisé)
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# AWS_REGION=eu-west-1
# S3_BUCKET=thot-uploads-production

# === CLEVER CLOUD SPECIFIC ===

# Health check path (optionnel, par défaut Clever Cloud vérifie juste que le port répond)
# CC_HEALTH_CHECK_PATH=/health

# Timeout pour le health check (en secondes)
# CC_HEALTH_CHECK_TIMEOUT=30

# Port HTTP exposé (par défaut 8080)
# CC_DOCKER_EXPOSED_HTTP_PORT=8080

# Port TCP exposé (si vous en avez besoin)
# CC_DOCKER_EXPOSED_TCP_PORT=9000

# Nom du Dockerfile (si vous n'utilisez pas "Dockerfile")
# CC_DOCKERFILE=Dockerfile.production

# Utiliser Docker Buildx
# CC_DOCKER_BUILDX=true

# Instance de build dédiée (pour les builds gourmands en mémoire)
# CC_DEDICATED_BUILD_INSTANCE=true

# === COMMANDES POUR CONFIGURER VIA CLI ===

# Installation de Clever Tools:
# npm install -g clever-tools
# clever login

# Lier votre application:
# cd backend
# clever link app_8a0fbd58-42ee-4661-bb09-fd1d484f8ad8

# Définir les variables (exemples):
# clever env set MONGO_URI "mongodb://..."
# clever env set JWT_SECRET "votre_secret_très_long_et_aléatoire"
# clever env set NODE_ENV "production"
# clever env set CLIENT_URL "https://votre-frontend.com"
# clever env set API_BASE_URL "https://app_8a0fbd58-42ee-4661-bb09-fd1d484f8ad8.cleverapps.io"

# Lister toutes les variables:
# clever env

# Supprimer une variable:
# clever env rm VARIABLE_NAME
